# Blocker Capsule

Ticket: `<ticket id>`
Phase: `<phase>`

## Minimal Facts

- Objective:
- Scope paths involved:
- Failed command or proof:
- Short evidence excerpt:
- Attempts already made:
- Policy boundary:
- Smallest proposed action:

## Exclusions

This capsule intentionally excludes raw transcripts, full logs, secrets,
`.env*`, forbidden paths, unrelated context, and unrelated local paths.

## Resolver Request

Classify the blocker and return authorization, action, validation, rollback,
and whether exactly one user question is required.
