schema_version: 2
ticket:
  id: TKT-2026-06-09-spec-artifact-package
  title: Add machine-readable SDD spec artifacts before implementation
  repo: agentic-engineering-workflow-template
  branch: main
  status: backlog
  created_at: '2026-06-09'
  owner_agent: manager
  ticket_class: implementation
  work_kind: feature
  phase: sdd-upgrade-v0.4.0
  target_version: 0.4.0
context:
  problem: The current workflow has strong tickets and proof gates, but tickets are doing too many jobs
    at once. There is no separate requirements/design/tasks artifact set that can act as the machine-readable
    Single Source of Truth for a feature.
  current_behavior: A worker can receive a scoped ticket with context, test_plan, proof_gates, and done_definition.
    The workflow does not require requirements.md, design.md, tasks.md, EARS/Given-When-Then acceptance
    criteria, invariants, or a traceable spec-to-test map.
  desired_behavior: Non-trivial work must start with a compact spec package. The ticket points to the
    spec package, and implementation workers treat it as source of truth together with AGENTS.md and agent
    memory. Small quick-flow tickets may explicitly justify why no full spec package is needed.
  relevant_prior_work:
  - 'User-provided iX article: Fünf Tools für Spec-driven Development im Überblick, 2026-06-08'
  - README.md
  - docs/workflow.md
  - templates/AGENTS.md.template
scope:
  goal: 'Create and wire templates for SDD feature/bugfix specs: requirements or bugfix analysis, design,
    and tasks with acceptance criteria and proof mapping.'
  allowed_files:
  - tickets/sdd-upgrade/TKT-2026-06-09-spec-artifact-package.yaml
  - templates/specs/**
  - templates/TEMPLATE.ticket.yaml
  - templates/TEMPLATE.orchestrator-ticket.yaml
  - checklists/ticket-readiness.md
  - docs/workflow.md
  - templates/AGENTS.md.template
  - prompts/manager-orchestrator.md
  - prompts/scoped-worker.md
  forbidden_files:
  - .env
  - .env.*
  - node_modules/**
  - dist/**
  - build/**
  - .next/**
  - coverage/**
  in_scope:
  - Add templates/specs/TEMPLATE.requirements.md with user stories, EARS-style acceptance, Given-When-Then
    cases, constraints, preconditions, postconditions, invariants, non-goals, and ambiguity log.
  - Add templates/specs/TEMPLATE.design.md with architecture, impacted files, interfaces, data flow, failure
    modes, security/privacy notes, and test strategy.
  - Add templates/specs/TEMPLATE.tasks.md with discrete, dependency-ordered tasks, owner role, allowed
    files, proof, and traceability to requirements.
  - Add spec_refs/spec_contract fields to ticket templates so every implementation ticket links to its
    spec artifacts or records a quick-flow exemption.
  - Update readiness checklist and role prompts so implementation cannot start until spec artifacts are
    present or exemption is explicit.
  out_of_scope:
  - Do not import GitHub Spec Kit or Kiro as a dependency.
  - Do not replace the existing ticket model.
  - Do not require full specs for tiny typo/docs changes when quick-flow exemption is valid.
execution:
  execution_intensity: expert_supported
  manager_role: Main Codex agent owns scope, verifies the work, and records proof in this ticket. Any
    Codex subagent inherits AGENTS.md, the active ticket scope, approval boundaries, forbidden actions,
    and verification requirements.
  worker_sequence:
  - role: read_only_reviewer
    mode: read_only
    task: Review the planned workflow/template changes for SDD usefulness, risk, and minimality before
      edits.
  - role: scoped_worker
    mode: implementation
    task: Implement only the scoped workflow/template change described in this ticket.
  expert_plan:
    required: true
    planning_reviewer: read-only Codex reviewer
    reviewer: final verifier
    record_location: execution_result
  debug_logging_plan:
    owner: scoped_worker
    surfaces:
    - YAML parse output
    - Markdown link/path inspection
    - grep output for version and old terminology
    temporary_logging_policy: Do not add temporary logs to template files; record command output only
      in ticket execution_result.
  test_plan:
  - Parse every changed YAML file with python -c "import yaml, pathlib; [yaml.safe_load(p.read_text())
    for p in pathlib.Path('.').rglob('*.yaml')]".
  - Run markdown/path consistency checks manually or with grep for all newly referenced template paths.
  - Check whether README, docs/workflow.md, AGENTS template, prompts, checklists, ticket templates, and
    agent memory files need updates and record the result.
  proof_gates:
  - Changed templates and tickets are internally consistent and parseable where applicable.
  - The workflow change is bounded to SDD/context-engineering support and does not alter product runtime
    assumptions.
  - Agent-memory update check is recorded.
  - Ticket result records changed files, commands, proof, skipped checks, blockers, and risks.
  - A new implementation ticket template exposes spec_refs or spec_contract fields.
  - Ticket readiness fails if non-trivial implementation work lacks either spec artifacts or a justified
    quick-flow exemption.
  - Spec templates remain concise enough to be copied into a target repo without product-specific assumptions.
  regression_gates:
  - No unrelated files changed.
  - No secrets, credentials, or private local paths are introduced.
  - No dependency installation, destructive cleanup, remote operation, release, deploy, or external-service
    action occurs without explicit approval.
  - Existing Codex scoped-worker/orchestrator safety boundaries remain stricter or equivalent after the
    change.
  visual_gate:
    required: false
    required_views: []
    pass_rule: Not required; this is a workflow/template change.
  checkpoint_policy: Create a scoped checkpoint commit only after proof passes or after explicit user
    approval.
  rollback_or_accept_policy: If the scoped change makes the workflow ambiguous or less safe, keep the
    ticket non-done, record the blocker, and propose a smaller follow-up ticket.
  stop_conditions:
  - The change would require product-repository assumptions that are not available in the template source.
  - The change requires editing forbidden files.
  - The design would weaken approval boundaries, proof gates, or scoped file ownership.
  - The work requires dependency installation, destructive commands, remote operations, deployment, release,
    secrets, migrations with side effects, network-dependent setup, or scope expansion without explicit
    approval.
  handoff_requirements:
  - Record changed files, commands run, proof result, skipped checks, blockers, risks, agent updates,
    and next action.
  done_definition: Done when the scoped workflow/template improvement is implemented, required proof has
    passed or justified skips are recorded, and execution_result is complete.
execution_result:
  status: done
  changed_files:
  - tickets/sdd-upgrade/TKT-2026-06-09-spec-artifact-package.yaml
  - templates/specs/TEMPLATE.requirements.md
  - templates/specs/TEMPLATE.design.md
  - templates/specs/TEMPLATE.tasks.md
  - templates/TEMPLATE.ticket.yaml
  - templates/TEMPLATE.orchestrator-ticket.yaml
  - checklists/ticket-readiness.md
  - docs/workflow.md
  - templates/AGENTS.md.template
  - prompts/manager-orchestrator.md
  - prompts/scoped-worker.md
  commands_run:
  - 'git status --short --branch'
  - 'sed -n ''1,240p'' tickets/sdd-upgrade/TKT-2026-06-09-spec-artifact-package.yaml'
  - 'rg --files templates checklists docs prompts | sort'
  - 'sed -n ''1,260p'' templates/TEMPLATE.ticket.yaml'
  - 'sed -n ''1,260p'' templates/TEMPLATE.orchestrator-ticket.yaml'
  - 'sed -n ''1,260p'' checklists/ticket-readiness.md'
  - 'sed -n ''1,280p'' docs/workflow.md'
  - 'sed -n ''1,320p'' templates/AGENTS.md.template'
  - 'sed -n ''1,320p'' prompts/manager-orchestrator.md'
  - 'sed -n ''1,320p'' prompts/scoped-worker.md'
  - 'find templates/specs -maxdepth 2 -type f -print 2>/dev/null | sort'
  - 'git diff -- templates/TEMPLATE.ticket.yaml templates/TEMPLATE.orchestrator-ticket.yaml checklists/ticket-readiness.md docs/workflow.md templates/AGENTS.md.template prompts/manager-orchestrator.md prompts/scoped-worker.md templates/specs/TEMPLATE.requirements.md templates/specs/TEMPLATE.design.md templates/specs/TEMPLATE.tasks.md'
  - 'rg -n "spec_refs|spec_contract|requirements\.md|design\.md|tasks\.md|quick-flow|quick flow|templates/specs" templates checklists docs prompts tickets/sdd-upgrade/TKT-2026-06-09-spec-artifact-package.yaml'
  - 'find templates/specs -maxdepth 1 -type f -print | sort'
  - 'python -c "import yaml, pathlib; [yaml.safe_load(p.read_text()) for p in pathlib.Path(''.'').rglob(''*.yaml'')]; print(''parsed yaml files'')"'
  - 'git diff --check'
  - 'rg -n "templates/specs/TEMPLATE\.(requirements|design|tasks)\.md|spec_refs|spec_contract|quick-flow exemption|requirements/design/tasks" templates checklists docs prompts tickets/sdd-upgrade/TKT-2026-06-09-spec-artifact-package.yaml'
  - 'rg -n "ticket|workflow|template|spec|requirements|design|tasks|AGENTS|agent memory" README.md agent 2>/dev/null'
  - 'git status --short'
  - 'git diff --name-only'
  - 'git diff -- tickets/sdd-upgrade/TKT-2026-06-09-spec-artifact-package.yaml | sed -n ''1,260p'''
  - 'python -c "import yaml; p=''tickets/sdd-upgrade/TKT-2026-06-09-spec-artifact-package.yaml''; data=yaml.safe_load(open(p)); print(data[''execution_result''][''status'']); print(len(data[''execution_result''][''changed_files''])); print(data[''execution_result''][''agent_memory_updates''][''checked'']))"'
  proof:
  - Added concise generic requirements, design, and tasks templates under templates/specs with acceptance
    criteria, Given-When-Then cases, constraints, invariants, ambiguity log, architecture, impacted files,
    interfaces, data flow, failure modes, security/privacy notes, task ownership, allowed files, proof,
    and requirement traceability.
  - Added spec_refs and spec_contract fields to implementation and orchestrator ticket templates, including
    quick-flow exemption support and traceability requirements.
  - Updated readiness checklist so non-trivial implementation fails readiness without linked specs or a
    justified quick-flow exemption.
  - Updated workflow docs, AGENTS template, manager prompt, and scoped-worker prompt to treat linked specs
    as source of truth while preserving existing scoped-worker and read-only expert routing boundaries.
  - 'YAML validation passed: python parsed every *.yaml file in the repository.'
  - 'Whitespace validation passed: git diff --check returned no output.'
  - 'Reference check passed: rg found the new spec contract and quick-flow references in the scoped templates,
    checklist, docs, prompts, and ticket.'
  skipped_checks:
  - README.md likely needs a follow-up installation-map mention for templates/specs, but README.md is outside
    this ticket allowed_files and was not edited.
  - No dependency, build, lint, or test command was run because this workflow/template change has no package
    install or runtime code path in scope; YAML parse, reference inspection, and git diff --check were the
    applicable proof gates.
  agent_memory_updates:
    checked: true
    updated_files: []
    notes: 'agent memory checked: no update needed; user explicitly forbade agent/*.md edits for this ticket,
      and this template-scope change is recorded in the ticket result instead.'
  blockers: []
  risks:
  - 'Risk: over-specification can slow tiny work; mitigate with quick-flow exemption and escalation ticket.'
  - 'README.md install-path documentation may lag the new spec templates until a separate docs-scoped ticket
    allows README edits.'
  next_recommended_step: 'Open a separate docs ticket for README.md if the install mapping should list templates/specs.'
